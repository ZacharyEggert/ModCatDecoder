{
  "name": "modcatdecoder",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
	"scripts": {
		"build": "tsc",
		"test": "bun test",
		"serve": "bunx http-server . -p 8080",
		"start": "cd ./frontend && bun run start",
		"build-frontend": "bun run build && bun run --cwd ./frontend build",
		"dev": "cd ./frontend && bun run dev"
	}
}
